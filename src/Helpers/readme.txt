if you have files with code that doesn't fit a specific folder and you want to have access to it from multiple files.
You can have the file/code in the Helpers folder.

for example if you have a code(file) to calculate the date and you will use that code in multiple files. The code(file) 
can go in the Helpers folder.